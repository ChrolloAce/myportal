{
  "name": "video-submission-dashboard",
  "version": "1.0.0",
  "description": "Streamlined dashboard for TikTok/Instagram video submissions",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm ci && npm run build && cp -r frontend/dist/* ./",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true
}